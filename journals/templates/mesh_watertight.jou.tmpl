; ============================================================
; @AERO-TEMPLATE mesh_watertight v0  (aeroharness journal 模板库 · M3 预置)
; 适用 Fluent : 2022R2(v222) / 2023R1(v231) Fluent Meshing（watertight 工作流）
; 状态        : ★ verified=false —— 本模板是骨架，watertight 各任务的
;               prompt 应答序列较长且随版本变化，必须在真机上用
;               TUI 录制（GUI: File → Write → Start Journal）反哺校准后
;               才允许批量使用（调研报告 6.8.1 反哺机制 / 6.7 风险表）。
; 用法        : fluent 后选择 Meshing Mode，或命令行
;               fluent 3d -meshing -t<N> -g -i journal.jou
; 校准方法    : 见 references/prompt_calibration.md 第 3 节（含逐任务录制步骤）
; ============================================================
; @EXPECTED-STEPS {{expected_steps}}

; ---- Step: 读入几何（STL/SCDOC/AGDB……）----
/file/read-mesh {{mesh_file}}
(display "; STEP-OK read_geometry")
(newline)

; ---- 以下任务序列需要按真机录制校准（骨架示例，禁止直接批量）----
; /workflow/initialize-workflow "Watertight Geometry Meshing Workflow"
; /workflow/task/set-active-task "Import Geometry"
; /workflow/task/update-child-tasks
; ...（表面网格/局部尺寸/边界层/体网格/质量检查各任务的参数与应答）
; (display "; STEP-OK watertight_mesh")
(newline)
; /mesh/check-mesh
; (display "; QUALITY min_orthogonal=... max_aspect=... neg_vol=...")
(newline)
; /file/write-mesh {{out_mesh}}
; (display "; STEP-OK write_mesh")
(newline)
; (display "; DONE")
(newline)
; exit
